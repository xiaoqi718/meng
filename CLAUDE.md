# meng - AI 简历优化器（项目上下文）

> 本文件记录项目当前状态、踩过的坑、设计决策，供后续继续优化时参考，避免重复犯错。

---

## 1. 项目概述

**meng** 是一个基于 Streamlit 的 AI 简历优化工具。用户上传 PDF 简历后，AI 给出：

- 简历评分（0-100）
- 主要问题（3-5 条）
- 修改建议（对应问题）
- 优化后的简历内容

当前部署在 **Streamlit Cloud**，大模型使用 **DeepSeek API**（兼容 OpenAI SDK）。

---

## 2. 技术栈与文件结构

### 技术栈

| 模块 | 技术 |
|------|------|
| 前端 | Streamlit 1.60.0 |
| 后端 | Python 3.13 |
| PDF 解析 | pdfplumber |
| 大模型 | DeepSeek API（deepseek-v4-flash 默认） |
| Word 导出 | python-docx（当前未使用，但保留） |
| 配置 | python-dotenv + Streamlit Secrets |
| 部署 | Streamlit Cloud（GitHub 自动同步） |

### 文件结构

```
meng/
├── app.py                      # Streamlit 前端主程序 + 自定义 CSS
├── requirements.txt            # Python 依赖
├── .env.example                # 环境变量示例
├── .streamlit/config.toml      # Streamlit 主题配置
├── README.md                   # 面向用户的使用说明
├── docs/PRD.md                 # 产品需求文档（较早，部分已过时）
├── CLAUDE.md                   # 本文件：项目上下文与踩坑记录
├── assets/background.png       # 页面背景图（星空主题）
├── src/
│   ├── __init__.py
│   ├── resume_parser.py        # PDF 文本提取
│   ├── resume_analyzer.py      # AI 分析 + SYSTEM_PROMPT
│   └── resume_exporter.py      # Word 导出（当前未使用）
└── examples/sample_resume.pdf  # 示例简历
```

---

## 3. 当前 Prompt 策略

### 当前状态

`src/resume_analyzer.py` 使用的是**直接重写完整简历**的 Prompt：

- AI 会输出标准结构的简历（个人信息 → 求职意向 → 教育背景 → 工作经历 → 项目经历 → 技能/证书）
- 保留真实信息，不编造经历
- 使用 STAR 法则，多用数字和结果
- 语气像真人 HR，避免 AI 腔

### 历史调整与教训

| 时间 | 改动 | 结果 |
|------|------|------|
| 最初 | 直接重写完整简历 | 输出比较通用，但结构标准 |
| 中间尝试 | 改成「基于原简历润色，最小化改动，保留原结构」 | 用户反馈改得太少、太死板 |
| 中间尝试 | 改成更像真人 HR 的口语化 Prompt | 用户决定回退 |
| **当前** | 回退到直接重写完整简历 | 用户接受此方向 |

**关键教训**：

- Prompt 的「保留原结构」和「最小化改动」容易让 AI 输出死板、缺乏个性。
- 「真人 HR 语气」和「结构化输出」之间存在张力，过度强调口语化可能导致格式不稳定。
- 用户最终选择**直接重写**方向，后续优化应在此基础上迭代，不要频繁在「重写」和「润色」之间摇摆。

### 优化方向建议

如果后续要改进 Prompt，建议：

1. **在「直接重写」基础上增加「保留用户亮点表达」**：让 AI 识别原简历中写得好的句子，直接保留。
2. **增加行业/岗位定向**：根据用户目标岗位调整优化重点（需要新增 UI 输入）。
3. **控制输出长度**：当前输出 sometimes 过长，可限制每条经历 2-4 点。
4. **避免 AI 套话**：继续禁用"首先/其次/综上所述/值得一提的是/希望以上建议对你有帮助"。

---

## 4. UI/UX 设计系统

### 当前风格

- **风格**：简约专业 SaaS，参考 Linear / Notion / Zety
- **背景**：星空主题背景图 + 半透明遮罩
- **主色**：`#2563EB`（专业蓝）
- **背景色**：`#F8FAFC`（浅灰蓝，作为 fallback）
- **卡片**：白色 + 细阴影 + 圆角 16px + 轻微毛玻璃效果
- **字体**：系统默认 sans-serif（PingFang SC / Microsoft YaHei）

### 页面结构

1. **Header**：meng logo + 标题 + 副标题
2. **上传卡片**：左说明 + 右上传区
3. **分析按钮**：居中，未上传时禁用
4. **进度条**：Streamlit 原生 `st.progress()`
5. **结果区**：
   - 评分卡片 + 评级 + 投递建议
   - Tabs：「💡 诊断分析」「✨ 优化后简历」
   - 诊断分析：折叠展示问题与建议
   - 优化后简历：格式化展示，无下载按钮

### 已移除的功能

- ❌ Minecraft 像素风 UI（因覆盖内容导致页面空白）
- ❌ 自定义 HTML 步骤条（因缩进被 Streamlit 识别为代码块，显示原始 HTML）
- ❌ Word 下载按钮（用户要求只显示分析结果和优化后简历）
- ❌ Markdown 下载按钮（同上）

### 重要 CSS 经验

1. **不要用自定义 HTML 做进度条/步骤条**：Streamlit 的 Markdown 会把缩进的 HTML 当成代码块显示。用 `st.progress()` 最稳。
2. **背景图要用 base64 内嵌**：Streamlit 对静态文件路径的支持有限，CSS 里直接引用 `assets/xxx` 不一定生效。当前方案是在 Python 中读取图片、base64 编码后注入 CSS。
3. **图片格式要检测**：用户可能把 PNG 保存为 `.jpg`，代码要自动检测真实格式（通过文件头 magic bytes），不要依赖扩展名。
4. **卡片背景加半透明 + 模糊**：背景图较亮时，卡片用 `rgba(255,255,255,0.96)` + `backdrop-filter: blur(8px)` 提升可读性。

---

## 5. 背景图处理

### 当前实现

- 图片路径：`assets/background.png`（也兼容 .jpg/.jpeg/.webp）
- 代码位置：`app.py` 顶部 `get_background_css()`
- 实现方式：读取图片 → base64 编码 → 注入 CSS 的 `background-image`
- 叠加一层半透明遮罩，保证文字可读性

### 注意事项

- 图片不要太大，建议控制在 500KB 以内，否则首次加载慢。
- 如果背景图太花、太亮，需要调低透明度或加深遮罩。
- 目前背景图尺寸 474×265，在全屏下会拉伸/裁切，如需更好的视觉效果，建议使用 1920×1080 或更高分辨率图。

---

## 6. 部署与配置

### 本地运行

```bash
cd D:/meng
pip install -r requirements.txt
streamlit run app.py
```

浏览器打开 `http://localhost:8501`

### Streamlit Cloud 部署

1. GitHub 仓库自动同步
2. 主文件路径：`app.py`
3. Secrets 配置：`DEEPSEEK_API_KEY = "your_key"`
4. 可选：`DEEPSEEK_MODEL = "deepseek-v4-pro"`（默认 deepseek-v4-flash）

### 主题配置

`.streamlit/config.toml`：

```toml
[theme]
primaryColor = "#2563EB"
backgroundColor = "#F8FAFC"
secondaryBackgroundColor = "#FFFFFF"
textColor = "#0F172A"
```

---

## 7. 常见错误与避坑指南

### 7.1 UI 类

| 错误 | 原因 | 解决方案 |
|------|------|---------|
| 页面空白，只显示黑色背景和绿色圆点 | 复杂 CSS 伪元素 / fixed 定位覆盖内容 | 简化背景，移除 fixed 定位伪元素，用 base64 背景图 |
| 步骤条显示原始 HTML 代码 | HTML 字符串带缩进，被 Markdown 识别为代码块 | 用 Streamlit 原生组件 `st.progress()` |
| 背景图不显示 | CSS 中引用本地路径不生效 | base64 编码后注入 CSS |
| 背景图太亮导致文字看不清 | 缺少遮罩或透明度不够 | 叠加 `rgba(248,250,252,0.88)` 遮罩 |

### 7.2 Prompt 类

| 错误 | 原因 | 解决方案 |
|------|------|---------|
| AI 输出太死板、太模板化 | 过度强调"保留结构"和"最小化改动" | 允许 AI 在核心经历上大胆改写 |
| AI 输出太 AI 腔 | 缺少具体语气示例和禁用词 | 明确禁用词列表，提供真实 HR 语气示例 |
| AI 编造数字/经历 | Prompt 没有明确禁止 | 明确写入"绝不编造"，并说明无数字时的处理方式 |
| 识别失败时 AI 硬分析 | 异常处理不够清晰 | 给出自然的人类化失败提示 |

### 7.3 导出类

| 错误 | 原因 | 解决方案 |
|------|------|---------|
| Word 中标题、列表格式错乱 | 导出逻辑只按行解析 | 解析 Markdown 标题层级、列表项、加粗 |
| 字体显示异常 | 默认字体不是中文友好字体 | 优先微软雅黑，回退宋体 |

---

## 8. 测试清单

每次修改后，按以下清单验证：

- [ ] 页面能正常打开，无报错
- [ ] 背景图正确显示，文字清晰可读
- [ ] 上传 PDF 后能成功提取文本
- [ ] 步骤条正常显示，不出现 HTML 代码
- [ ] AI 输出包含：评分、主要问题、修改建议、优化后简历
- [ ] 优化后简历有 `===OPTIMIZED_RESUME_START===` / `===OPTIMIZED_RESUME_END===` 标记
- [ ] 诊断分析和优化后简历两个 tab 能正常切换
- [ ] AI 没有编造公司、职位、时间、数字
- [ ] 未上传文件时点击分析按钮有友好提示
- [ ] 非简历 PDF 上传后给出识别失败提示

---

## 9. 后续可优化方向

### 短期（1-2 周内）

1. **Prompt 调优**：在「直接重写」基础上，让 AI 识别并保留原简历中的亮点句子。
2. **背景图优化**：换更高分辨率、更适配全屏的背景图，或提供多张可选。
3. **移动端适配**：当前最大宽度 960px，移动端需要测试卡片和 tab 的显示。
4. **结果可复制性**：在优化后简历卡片右上角加一个「一键复制」按钮。

### 中期（1-2 月内）

1. **岗位 JD 定向优化**：增加输入框让用户粘贴目标岗位描述，AI 据此调整简历重点。
2. **Word/PDF 导出重新设计**：如果用户需要导出，提供高质量、可投递的模板。
3. **多轮对话优化**：允许用户对某一段经历提出"再改一下"的细化要求。
4. **历史记录**：保存用户上传和分析历史（需用户系统）。

### 长期

1. 用户系统与支付
2. 行业/岗位模板库
3. 面试模拟功能

---

## 10. 关键联系人与决策记录

- **项目 owner**：用户（AI 初学者，希望从 0 到 1 落地并商业化）
- **当前设计风格**：专业 SaaS + 星空背景
- **当前 Prompt 方向**：直接重写完整简历
- **不做的功能**：登录/支付/数据库（当前阶段）

---

**最后更新**：2026-07-25
