# meng - AI 简历优化器（项目上下文）

> 本文件记录项目当前状态、踩过的坑、设计决策，供后续继续优化时参考，避免重复犯错。

---

## 1. 项目概述

**meng** 是一个基于 Streamlit 的 AI 简历优化工具。用户上传 PDF 简历后，AI 给出：

- 简历评分（0-100）
- 主要问题（3-5 条）
- 修改建议（对应问题）
- 优化后的简历内容

当前部署在 **Streamlit Cloud**，大模型使用 **DeepSeek API**（兼容 OpenAI SDK）。

---

## 2. 技术栈与文件结构

### 技术栈

| 模块 | 技术 |
|------|------|
| 前端 | Streamlit 1.60.0 |
| 后端 | Python 3.13 |
| PDF 解析 | pdfplumber |
| 大模型 | DeepSeek API（deepseek-v4-flash 默认） |
| Word 导出 | python-docx（当前未使用，但保留） |
| 配置 | python-dotenv + Streamlit Secrets |
| 部署 | Streamlit Cloud（GitHub 自动同步） |

### 文件结构

```
meng/
├── app.py                      # Streamlit 前端主程序 + 自定义 CSS
├── requirements.txt            # Python 依赖
├── .env.example                # 环境变量示例
├── .streamlit/config.toml      # Streamlit 主题配置
├── README.md                   # 面向用户的使用说明
├── docs/PRD.md                 # 产品需求文档（较早，部分已过时）
├── CLAUDE.md                   # 本文件：项目上下文与踩坑记录
├── assets/background.png       # 页面背景图（星空主题）
├── src/
│   ├── __init__.py
│   ├── resume_parser.py        # PDF 文本提取
│   ├── resume_analyzer.py      # AI 分析 + SYSTEM_PROMPT
│   └── resume_exporter.py      # Word 导出（当前未使用）
└── examples/sample_resume.pdf  # 示例简历
```

---

## 3. 当前 Prompt 策略

### 当前状态

`src/resume_analyzer.py` 使用**直接重写完整简历 + 逐段修改块（CHANGE_BLOCK）**的 Prompt，人设为：

- **李明**，前字节跳动 HRBP，现某 C 轮 AI 公司招聘负责人
- 10 年以上互联网 / 快消 / 硬科技行业招聘与人才顾问经验
- 经手 5 万+ 简历，3000+ 面试
- 从 HR 和业务负责人双视角分析

Prompt 强调的分析视角：

1. **10 秒扫读测试**：HR 只看 6-10 秒，必须明确指出 10 秒内能看到的关键词/数字，以及找不到的信息。
2. **ATS 现实**：简历需要同时被招聘系统和 HR 通过，使用标准标题、关键词自然匹配，避免表格/图片/分栏。
3. **行业针对性**：根据简历推断目标行业（互联网产品 / 技术 / 快消市场 / 运营 / 金融等），给出该行业 HR 第一眼检查点。
4. **招聘心理**：说明简历在同类候选人中的位置，列出真实的怀疑/淘汰信号（跳槽无解释、只写职责不写成果、技能与经历不匹配等）。
5. **保留亮点**：识别原简历中写得好的句子或数据，直接保留，并在 REASON 中说明保留原因。

输出格式要求：

- `===CHANGE_BLOCK_START===...===CHANGE_BLOCK_END===`：每个需要修改的段落一个块，包含 `SECTION` / `ORIGINAL` / `MODIFIED` / `REASON`。
- `===OPTIMIZED_RESUME_START===...===OPTIMIZED_RESUME_END===`：完整优化后的简历。
- REASON 必须包含三段式：`原句问题` / `HR/业务负责人反应` / `改后预期`。

### 历史调整与教训

| 时间 | 改动 | 结果 |
|------|------|------|
| 最初 | 直接重写完整简历 | 输出比较通用，但结构标准 |
| 中间尝试 | 改成「基于原简历润色，最小化改动，保留原结构」 | 用户反馈改得太少、太死板 |
| 中间尝试 | 改成更像真人 HR 的口语化 Prompt | 用户决定回退 |
| 中间尝试 | 在直接重写基础上强化 10 年资深 HR + 行业专家视角 | 输出仍有空洞感 |
| 最近 | 给角色注入具体工作场景、给视角注入可验证信号、REASON 三段式、语气分层 | 当前采用方向 |

**关键教训**：

- Prompt 的「保留原结构」和「最小化改动」容易让 AI 输出死板、缺乏个性。
- 「真人 HR 语气」和「结构化输出」之间存在张力：过度口语化会破坏格式，完全结构化又会像 AI。
- **解决方案**：把两种语气分开。诊断部分（REASON）可以口语化；交付部分（MODIFIED / 优化后简历）必须是干净职业简历语言。
- 角色设定要有具体场景锚点（公司类型、招聘工具、典型一天），否则容易泛泛而谈。
- 分析视角要给出可验证的具体信号，否则 AI 会输出空洞总结。
- CHANGE_BLOCK 的 REASON 字段必须在 UI 中展示出来，否则 HR 视角的洞察用户看不到。

---

## 4. UI/UX 设计系统

### 当前风格

- **风格**：深色玻璃拟态（glassmorphism）SaaS，参考 Linear / Vercel / 现代 AI 工具
- **背景**：星空主题背景图 + 深色径向渐变遮罩
- **主色**：`#3B82F6` → `#6366F1` 渐变
- **页面背景**：深色渐变 `#020617` → `#0F172A`
- **卡片**：半透明深色 `rgba(15,23,42,0.60)` + 毛玻璃 `backdrop-filter: blur(16px)` + 细白边
- **文字**：白色标题 `#F8FAFC`，浅灰正文 `#CBD5E1`

### 页面结构

1. **Hero 区域**：复刻简历精修优化页风格
   - 左上角「简历精修」badge、右上角「面试官视角」胶囊
   - 英文 eyebrow：`RESUME OPTIMIZATION`
   - 主标题：`简历精修优化`
   - 副标题：`让你的简历 · 不再石沉大海`
   - 统计区：`AI 实时分析` / `3 步完成` / `24h 随时可用`
2. **上传区**：居中的玻璃虚线拖拽区
3. **分析按钮**：居中渐变按钮
4. **步骤卡片**：三个横向玻璃卡片（全面诊断 / 精修优化 / 岗位匹配），分析过程中依次高亮
5. **结果区**：
   - 大号评分数字 + 评级 + 评分理由 + 投递建议
   - **逐段精修对比**：每个 CHANGE_BLOCK 渲染成一张卡片，左栏「改前原文」、右栏「改后优化」、底部「HR 判断」
   - **完整优化后简历**：独立卡片展示 + 右上角「复制」按钮

### 已移除的功能

- ❌ Minecraft 像素风 UI
- ❌ 自定义 HTML 步骤条（显示原始 HTML 的 bug）
- ❌ Streamlit 原生 `st.progress()` 进度条
- ❌ Word / Markdown 下载按钮
- ❌ 大白色卡片
- ❌ 用编号列表展示问题与建议（改为改前改后对比卡片）

### 重要 CSS / UI 经验

1. **自定义 HTML 步骤卡片可行，但必须无缩进**：Streamlit 的 Markdown 会把缩进的 HTML 当成代码块显示。生成 HTML 字符串时不要带前导缩进，直接作为一行 compact string 传入 `st.markdown(..., unsafe_allow_html=True)`。
2. **背景图要用 base64 内嵌**：Streamlit 对静态文件路径的支持有限，CSS 里直接引用 `assets/xxx` 不一定生效。当前方案是在 Python 中读取图片、base64 编码后注入 CSS。
3. **图片格式要检测**：用户可能把 PNG 保存为 `.jpg`，代码要自动检测真实格式（通过文件头 magic bytes），不要依赖扩展名。
4. **深色背景 + 玻璃卡片**：比浅色白卡片更适合有背景图的场景，能减少"大白色板"的感觉。
5. **一键复制用原生 JS**：通过 `st.components.v1.html` 注入 `navigator.clipboard.writeText()`，零依赖、样式可控，并配 `document.execCommand('copy')` 降级。
6. **页面使用 `layout="wide"`**：改前改后对比需要宽屏空间，同时保留响应式断点，手机端自动换行。
7. **用户简历文本必须 `html.escape()`**：在把 ORIGINAL / MODIFIED 注入自定义 HTML 前必须转义，否则特殊字符会破坏 DOM。
8. **session_state 状态机驱动步骤卡片**：`idle → parsing → analyzing → done`，每阶段先渲染 UI 再执行阻塞任务，然后 `st.rerun()` 进入下一阶段。这样用户能看到步骤卡片依次激活，同时避免重复调用 API。

---

## 5. 背景图处理

### 当前实现

- 图片路径：`assets/background.png`（也兼容 .jpg/.jpeg/.webp）
- 代码位置：`app.py` 顶部 `get_background_css()`
- 实现方式：读取图片 → base64 编码 → 注入 CSS 的 `background-image`
- 叠加：深色径向渐变遮罩，让星星成为环境纹理
- 固定：`background-attachment: fixed`

### 注意事项

- 图片不要太大，建议控制在 500KB 以内，否则首次加载慢。
- 如果背景图太花、太亮，需要加深遮罩或降低透明度。
- 目前背景图尺寸 474×265，在全屏下会拉伸，如需更好效果，建议换 1920×1080 或更高分辨率图。

---

## 6. 部署与配置

### 本地运行

```bash
cd D:/meng
pip install -r requirements.txt
streamlit run app.py
```

浏览器打开 `http://localhost:8501`

### Streamlit Cloud 部署

1. GitHub 仓库自动同步
2. 主文件路径：`app.py`
3. Secrets 配置：`DEEPSEEK_API_KEY = "your_key"`
4. 可选：`DEEPSEEK_MODEL = "deepseek-v4-pro"`（默认 deepseek-v4-flash）

### 主题配置

`.streamlit/config.toml`：

```toml
[theme]
primaryColor = "#3B82F6"
backgroundColor = "#020617"
secondaryBackgroundColor = "#0F172A"
textColor = "#F8FAFC"
```

---

## 7. 常见错误与避坑指南

### 7.1 UI 类

| 错误 | 原因 | 解决方案 |
|------|------|---------|
| 页面空白，只显示黑色背景和绿色圆点 | 复杂 CSS 伪元素 / fixed 定位覆盖内容 | 简化背景，移除 fixed 定位伪元素，用 base64 背景图 |
| 步骤条显示原始 HTML 代码 | HTML 字符串带缩进，被 Markdown 识别为代码块 | 用 Streamlit 原生组件 `st.progress()` |
| 背景图不显示 | CSS 中引用本地路径不生效 | base64 编码后注入 CSS |
| 背景图太亮导致文字看不清 | 缺少遮罩或透明度不够 | 叠加深色径向渐变遮罩 |
| 界面像"大白板" | 使用大块纯白卡片 | 改用半透明玻璃拟态卡片 |
| 复制按钮不工作 | 用了 `pyperclip` 或没处理 http 环境 | 用原生 JS Clipboard API + `execCommand` 降级 |

### 7.2 Prompt 类

| 错误 | 原因 | 解决方案 |
|------|------|---------|
| AI 输出太死板、太模板化 | 过度强调"保留结构"和"最小化改动" | 允许 AI 在核心经历上大胆改写 |
| AI 输出太 AI 腔 | 缺少具体语气示例和禁用词 | 明确禁用词列表，提供真实 HR 语气示例 |
| AI 编造数字/经历 | Prompt 没有明确禁止 | 明确写入"绝不编造"，并说明无数字时的处理方式 |
| 识别失败时 AI 硬分析 | 异常处理不够清晰 | 给出自然的人类化失败提示 |
| 分析缺乏深度 | 只有通用建议，没有行业视角 | 加入行业推断、招聘心理、ATS 现实等视角 |

### 7.3 导出类

| 错误 | 原因 | 解决方案 |
|------|------|---------|
| Word 中标题、列表格式错乱 | 导出逻辑只按行解析 | 解析 Markdown 标题层级、列表项、加粗 |
| 字体显示异常 | 默认字体不是中文友好字体 | 优先微软雅黑，回退宋体 |

---

## 8. 测试清单

每次修改后，按以下清单验证：

- [ ] 页面能正常打开，无报错
- [ ] 背景图正确显示，文字清晰可读
- [ ] Hero 区域显示「简历精修优化」标题、统计区、三个步骤卡片
- [ ] 没有大白色卡片，卡片是玻璃拟态效果
- [ ] 上传 PDF 后能成功提取文本
- [ ] 步骤卡片正常显示，不出现 HTML 代码
- [ ] 分析过程中步骤卡片依次高亮（第 1 步 → 第 2 步 → 第 3 步）
- [ ] AI 输出包含：评分、CHANGE_BLOCK、优化后简历
- [ ] CHANGE_BLOCK 被正确解析为改前改后对比卡片
- [ ] 改前卡片里的原文与上传简历一致
- [ ] HR 判断（REASON）展示在改后卡片下方
- [ ] 优化后简历有 `===OPTIMIZED_RESUME_START===` / `===OPTIMIZED_RESUME_END===` 标记
- [ ] 一键复制按钮能把优化后简历完整复制到剪贴板
- [ ] AI 没有编造公司 / 职位 / 时间 / 数字
- [ ] AI 分析有行业针对性和招聘心理洞察
- [ ] 评分有理有据，不轻易给 80+ 讨好型打分
- [ ] 未上传文件时点击分析按钮有友好提示
- [ ] 非简历 PDF 上传后给出识别失败提示
- [ ] 刷新页面不会重复调用 API（结果保存在 session_state）
- [ ] 单元测试 `pytest tests/test_app_parser.py` 全部通过

---

## 9. 后续可优化方向

### 短期（1-2 周内）

1. **Prompt 持续调优**：根据用户反馈，调整评分校准、REASON 三段式输出质量、行业信号密度。
2. **背景图优化**：换更高分辨率、更适配全屏的背景图。
3. **移动端适配**：当前最大宽度已改为 1400px 宽屏布局，需要测试移动端显示。
4. **复制按钮增强**：支持复制诊断分析内容，或提供「复制全部」选项。
5. **面试官视角开关**：右上角的「面试官视角」目前是展示性 badge，可考虑做成真正的视图切换（显示/隐藏 HR 判断细节）。

### 中期（1-2 月内）

1. **岗位 JD 定向优化**：增加输入框让用户粘贴目标岗位描述，AI 据此调整简历重点。
2. **Word/PDF 导出重新设计**：如果用户需要导出，提供高质量、可投递的模板。
3. **多轮对话优化**：允许用户对某一段经历提出"再改一下"的细化要求。
4. **历史记录**：保存用户上传和分析历史（需用户系统）。

### 长期

1. 用户系统与支付
2. 行业/岗位模板库
3. 面试模拟功能

---

## 10. 关键联系人与决策记录

- **项目 owner**：用户（AI 初学者，希望从 0 到 1 落地并商业化）
- **当前设计风格**：深色玻璃拟态 + 星空背景 + 简历精修页风格（顶部 badge、统计区、三个步骤卡片、改前改后对比）
- **当前 Prompt 方向**：直接重写完整简历 + CHANGE_BLOCK 逐段修改 + 具体场景锚定的资深 HR / 行业专家视角
- **当前 UI 方向**：`layout="wide"` 宽屏布局，结果以改前改后对比卡片展示，不用编号列表
- **不做的功能**：登录/支付/数据库（当前阶段）

---

**最后更新**：2026-07-25
